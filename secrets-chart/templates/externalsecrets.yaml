{{- /*
  ExternalSecrets → Kubernetes Secrets (ESO).
  ASM JSON shapes (bootstrap outside Terraform — never in TF state):

  postgresql-admin:  {"username":"...","password":"...","database":"..."}
  postgresql-app:    {"username":"...","password":"...","database":"..."}
  flagd-ui:          {"SECRET_KEY_BASE":"...","FLAGD_SYNC_TOKEN":"..."}
  product-reviews:   {"OPENAI_API_KEY":"..."}
  grafana:           {"admin-user":"...","admin-password":"..."}
  opensearch:        {"username":"...","password":"..."}
    - username: admin user for OpenSearch security plugin (ADMIN role)
    - password: admin password (alphanumeric, length ≥ 24)
    Two derived keys are rendered by ESO template:
      OPENSEARCH_INITIAL_ADMIN_PASSWORD  → opensearch pod (bootstrap admin)
      username / password                → otel-collector HTTP basic auth

  DSN templates require alphanumeric passwords only while strings are concatenated.

  Argo CD tracking: every target uses template.engineVersion v2 so ESO does NOT
  copy ExternalSecret labels/annotations (including argocd.argoproj.io/instance
  and Helm meta) onto the generated Secret. Without a template, ESO merges ES
  metadata onto the Secret and Argo marks those Secrets OutOfSync/requiresPruning
  even though prune is false. When template is set with empty template.data,
  ESO still materializes keys from spec.data / dataFrom (MergePolicy default).
*/ -}}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.postgresqlAdmin }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: postgresql-admin
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.postgresqlAdmin }}
    creationPolicy: {{ .Values.creationPolicy }}
    # template present → do not inherit Argo/Helm labels onto the K8s Secret
    template:
      engineVersion: v2
  data:
    - secretKey: POSTGRES_USER
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/postgresql-admin
        property: username
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/postgresql-admin
        property: password
    - secretKey: POSTGRES_DB
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/postgresql-admin
        property: database
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.postgresqlApp }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: postgresql-app
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.postgresqlApp }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
      data:
        username: {{ `"{{ .username }}"` }}
        password: {{ `"{{ .password }}"` }}
        database: {{ `"{{ .database }}"` }}
        accounting-db-connection-string: {{ `"Host=postgresql;Username={{ .username }};Password={{ .password }};Database={{ .database }}"` }}
        product-catalog-db-connection-string: {{ `"postgres://{{ .username }}:{{ .password }}@postgresql/{{ .database }}?sslmode=disable"` }}
        product-reviews-db-connection-string: {{ `"host=postgresql user={{ .username }} password={{ .password }} dbname={{ .database }}"` }}
  dataFrom:
    - extract:
        key: {{ .Values.remoteKeyPrefix }}/postgresql-app
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.flagdUi }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: flagd-ui
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.flagdUi }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: SECRET_KEY_BASE
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/flagd-ui
        property: SECRET_KEY_BASE
    - secretKey: FLAGD_SYNC_TOKEN
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/flagd-ui
        property: FLAGD_SYNC_TOKEN
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.productReviews }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: product-reviews
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.productReviews }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: OPENAI_API_KEY
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/product-reviews
        property: OPENAI_API_KEY
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.grafanaAdmin }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: grafana-admin
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.grafanaAdmin }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: admin-user
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/grafana
        property: admin-user
    - secretKey: admin-password
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/grafana
        property: admin-password
---
# SEC-06: OpenSearch security plugin credentials
# ASM shape: {"username":"admin","password":"<strong-alphanumeric>"}
# Produces two keys consumed by different pods:
#   OPENSEARCH_INITIAL_ADMIN_PASSWORD → opensearch StatefulSet (bootstrap admin on first start)
#   username / password               → otel-collector HTTP basic auth (write-only path)
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.opensearch }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: opensearch
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.opensearch }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: username
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/opensearch
        property: username
    - secretKey: password
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/opensearch
        property: password
    - secretKey: OPENSEARCH_INITIAL_ADMIN_PASSWORD
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/opensearch
        property: password
{{- if and .Values.valkeyCart .Values.valkeyCart.enabled .Values.valkeyCart.remoteKey }}
---
# Directive #3: ElastiCache Valkey auth token (transit encryption + AUTH).
# ASM shape (Terraform commerce_ha): {"password":"<auth-token>"}
# Consumed by cart as VALKEY_PASSWORD when VALKEY_TLS=true.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.valkeyCart }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: valkey-cart
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.valkeyCart }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: VALKEY_PASSWORD
      remoteRef:
        key: {{ .Values.valkeyCart.remoteKey }}
        property: password
{{- end }}
---
# Grafana Discord alert webhook (replaces kubectl create secret — SEC-05)
# ASM shape: {"webhook-url":"https://discord.com/api/webhooks/<id>/<token>"}
# Bootstrap: DISCORD_WEBHOOK_URL=<url> ./scripts/bootstrap-asm-secrets.sh <prefix>
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.grafanaDiscord }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: grafana-discord
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.grafanaDiscord }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: webhook-url
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/grafana-discord
        property: webhook-url
---
# AIOps inbound Grafana webhook auth secret.
# ASM shape: {"AIOPS_GRAFANA_WEBHOOK_SECRET":"<random-secret>"}
# Bootstrap: AIOPS_GRAFANA_WEBHOOK_SECRET="$(openssl rand -hex 32)" ./scripts/bootstrap-asm-secrets.sh <prefix>
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.aiopsGrafanaWebhook }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: aiops-grafana-webhook
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.aiopsGrafanaWebhook }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: AIOPS_GRAFANA_WEBHOOK_SECRET
      remoteRef:
        key: {{ .Values.remoteKeyPrefix }}/aiops-grafana-webhook
        property: AIOPS_GRAFANA_WEBHOOK_SECRET
{{- if and .Values.msk .Values.msk.enabled .Values.msk.remoteKey }}
---
# Directive #8: MSK bootstrap brokers TLS endpoints.
# ASM shape (Terraform msk module): {"brokers":"<bootstrap-brokers>"}
# Consumed by checkout, accounting, fraud-detection as KAFKA_ADDR when KAFKA_TLS=true.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.targets.msk }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: techx-corp-secrets
    app.kubernetes.io/component: msk
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.targets.msk }}
    creationPolicy: {{ .Values.creationPolicy }}
    template:
      engineVersion: v2
  data:
    - secretKey: KAFKA_ADDR
      remoteRef:
        key: {{ .Values.msk.remoteKey }}
        property: brokers
{{- end }}
{{/* Change trail: @hungxqt - 2026-07-15 - Added flagd sync token mapping from AWS Secrets Manager. */}}
{{/* Change trail: @hungxqt - 2026-07-16 - Add ESO target.template on all ExternalSecrets to stop Argo tracking labels on generated Secrets. */}}
