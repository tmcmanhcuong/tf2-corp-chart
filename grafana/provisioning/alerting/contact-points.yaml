# Copyright TechX Corp Platform Team
# SPDX-License-Identifier: Apache-2.0
#
# Grafana provisioning — Contact Points
#
# Required env var — inject vào values.yaml grafana.envValueFrom:
#   DISCORD_WEBHOOK_URL : webhook URL từ Discord Server Settings → Integrations → Webhooks
#                         dạng: https://discord.com/api/webhooks/<id>/<token>
#
# Tạo secret trước khi deploy:
#   kubectl create secret generic techx-corp-grafana-discord \
#     --from-literal=webhook-url=https://discord.com/api/webhooks/<id>/<token> \
#     -n techx-corp-prod
---
apiVersion: 1

contactPoints:
  - orgId: 1
    name: discord-alerts
    receivers:
      - uid: discord-alerts-receiver
        type: discord
        settings:
          url: "${DISCORD_WEBHOOK_URL}"
          # Dùng @everyone mention cho critical nếu muốn — để trống để không mention
          use_discord_username: false
        disableResolveMessage: false
