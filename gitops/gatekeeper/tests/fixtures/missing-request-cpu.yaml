apiVersion: v1
kind: Pod
metadata: {name: missing-request-cpu, namespace: default}
spec:
  containers:
    - name: app
      image: nginx:1.29.0
      resources:
        requests: {memory: 16Mi}
        limits: {cpu: 100m, memory: 64Mi}
