apiVersion: batch/v1
kind: CronJob
metadata: {name: invalid-cronjob, namespace: default}
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - {name: app, image: "nginx:latest"}
