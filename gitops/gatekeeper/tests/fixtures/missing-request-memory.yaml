apiVersion: v1
kind: Pod
metadata: {name: missing-request-memory, namespace: default}
spec:
  containers:
    - name: app
      image: nginx:1.29.0
      resources:
        requests: {cpu: 10m}
        limits: {cpu: 100m, memory: 64Mi}
