apiVersion: v1
kind: Pod
metadata: {name: missing-drop-all, namespace: default}
spec:
  securityContext:
    runAsNonRoot: true
  containers:
    - name: app
      image: nginx:1.29.0
      securityContext:
        capabilities:
          drop: ["NET_RAW"]
