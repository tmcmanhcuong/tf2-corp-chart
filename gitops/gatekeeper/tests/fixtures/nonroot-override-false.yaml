apiVersion: v1
kind: Pod
metadata: {name: nonroot-override-false, namespace: default}
spec:
  securityContext:
    runAsNonRoot: true
  containers:
    - name: app
      image: nginx:1.29.0
      securityContext:
        runAsNonRoot: false
        capabilities:
          drop: ["ALL"]
