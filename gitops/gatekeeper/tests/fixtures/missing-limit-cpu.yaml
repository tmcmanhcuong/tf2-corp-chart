apiVersion: v1
kind: Pod
metadata: {name: missing-limit-cpu, namespace: default}
spec:
  containers:
    - name: app
      image: nginx:1.29.0
      resources:
        requests: {cpu: 10m, memory: 16Mi}
        limits: {memory: 64Mi}
