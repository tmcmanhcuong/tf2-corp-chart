apiVersion: v1
kind: Service
metadata:
  name: policy-controller
  namespace: cosign-system
  labels:
    app.kubernetes.io/name: policy-controller
spec:
  ports:
    - name: https-webhook
      port: 443
      targetPort: 8443
  selector:
    app.kubernetes.io/name: policy-controller
